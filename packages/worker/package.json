{
  "name": "@stamped/l6-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "L6 pg-boss worker — reports, schedules, webhooks (no Redis)",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node --import tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test tests/**/*.test.ts",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "pg-boss": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
